<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.sunshine">

    <application
        android:allowBackup="true"
        android:icon="@drawable/art_clear"
        android:label="@string/app_name"
        android:roundIcon="@drawable/art_clear"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UpravPocasie"></activity>
        <activity android:name=".UpravMesto" />
        <activity android:name=".MestoZmen" />
        <activity android:name=".PocasieDetails" />
        <activity android:name=".PridajPocasie" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>